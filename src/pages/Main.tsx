import React from 'react';
import Task from "../components/Task/Task";

const Main = (props:any) => {
    return (
        <div>
            <Task/>
            <Task/>
            <Task/>
        </div>
    );
};

export default Main;
